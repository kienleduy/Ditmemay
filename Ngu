local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/jensonhirst/Orion/main/source')))()
local Window = OrionLib:MakeWindow({
    Name = "ALL-IN-ONE MENU 👺",
    HidePremium = true,
    SaveConfig = false,
    IntroEnabled = false
})

local MainTab = Window:MakeTab({Name = "Main"})
local QuestTab = Window:MakeTab({Name = "Auto Quest"})
local SkillTab = Window:MakeTab({Name = "Auto Skill"})
local FruitTab = Window:MakeTab({Name = "Fruit"})
local RandomTab = Window:MakeTab({Name = "Random & Lucky"})
local MiscTab = Window:MakeTab({Name = "Misc"})

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local function safe(func)
    pcall(func)
end

-- [KILL AURA - giữ nguyên]
local killAuraEnabled = false
local selectedTargetName = "Chưa chọn"
local targetList = {"Chưa chọn", "Siu_Owner"}
local dropdown

local function refreshTargetList()
    targetList = {"Chưa chọn", "Siu_Owner"}
    local npcFolder = workspace:FindFirstChild("NPC DAMAGE")
    if npcFolder then
        for _, model in ipairs(npcFolder:GetChildren()) do
            if model:IsA("Model") and model:FindFirstChildWhichIsA("Humanoid") then
                if model.Name ~= "Siu_Owner" then 
                    table.insert(targetList, model.Name) 
                end
            end
        end
    end
    if dropdown then dropdown:Refresh(targetList, true) end
end

local function killTarget(model)
    if not model then return end
    local hum = model:FindFirstChildWhichIsA("Humanoid")
    if hum and hum.Health > 0 then
        task.spawn(function()
            for i = 1, 25 do
                if hum.Health <= 0 then break end
                safe(function()
                    hum.Health = 0
                    hum.MaxHealth = 0
                    hum.Health = -math.huge
                    hum:TakeDamage(9e9)
                    hum:ChangeState(Enum.HumanoidStateType.Dead)
                end)
                task.wait()
            end
        end)
    end
end

MainTab:AddToggle({
    Name = "Kích hoạt Kill Aura (Siu & NPC - Kill từ xa)",
    Default = false,
    Callback = function(state)
        killAuraEnabled = state
        if killAuraEnabled then
            OrionLib:MakeNotification({Name = "KILL AURA", Content = "BẬT - Kill từ xa: " .. selectedTargetName, Time = 5})
            task.spawn(function()
                while killAuraEnabled do
                    task.wait(0.08)
                    safe(function()
                        sethiddenproperty(LocalPlayer, "SimulationRadius", 1e12)
                        sethiddenproperty(LocalPlayer, "MaxSimulationRadius", 1e12)
                    end)
                    
                    if selectedTargetName == "Siu_Owner" then
                        local boss = workspace:FindFirstChild("Siu_Owner")
                        if boss then killTarget(boss) end
                    else
                        local npcFolder = workspace:FindFirstChild("NPC DAMAGE")
                        if npcFolder then
                            local target = npcFolder:FindFirstChild(selectedTargetName)
                            if target then killTarget(target) end
                        end
                    end
                end
            end)
        else
            OrionLib:MakeNotification({Name = "KILL AURA", Content = "TẮT", Time = 3})
        end
    end    
})

dropdown = MainTab:AddDropdown({
    Name = "Chọn mục tiêu",
    Default = "Chưa chọn",
    Options = targetList,
    Callback = function(v)
        selectedTargetName = v
        OrionLib:MakeNotification({Name = "Đã chọn", Content = "Mục tiêu: " .. v, Time = 4})
    end
})

MainTab:AddButton({Name = "Làm mới danh sách quái", Callback = function() refreshTargetList() end})

-- [AUTO QUEST - QUAY VỀ KIỂU CŨ: QUÉT GUI QuestTake, fire Accept0 & Accept1]
local autoQuestEnabled = false

QuestTab:AddToggle({
    Name = "Auto Nhận Quest (Accept 0 & 1)",
    Default = false,
    Callback = function(state)
        autoQuestEnabled = state
        
        if autoQuestEnabled then
            OrionLib:MakeNotification({
                Name = "AUTO QUEST",
                Content = "BẬT - Tự động nhận Accept0 & Accept1 khi GUI QuestTake hiện",
                Time = 5
            })
        else
            OrionLib:MakeNotification({
                Name = "AUTO QUEST",
                Content = "TẮT",
                Time = 3
            })
        end
        
        task.spawn(function()
            while autoQuestEnabled do
                task.wait(0.1)  -- delay 0.5s như bạn yêu cầu
                
                safe(function()
                    local playerGui = LocalPlayer:WaitForChild("PlayerGui")
                    local questGui = playerGui:FindFirstChild("QuestTake")
                    
                    if questGui then
                        -- Fire Accept0 nếu có
                        local accept0 = questGui:FindFirstChild("Accept0")
                        if accept0 then
                            local re0 = accept0:FindFirstChildOfClass("RemoteEvent")
                            if re0 then re0:FireServer() end
                        end
                        
                        -- Fire Accept1 nếu có
                        local accept1 = questGui:FindFirstChild("Accept1")
                        if accept1 then
                            local re1 = accept1:FindFirstChildOfClass("RemoteEvent")
                            if re1 then re1:FireServer() end
                        end
                    end
                end)
            end
        end)
    end    
})

-- Nút test thủ công Accept0 & Accept1
QuestTab:AddButton({
    Name = "Nhận Quest Ngay (Accept 0 & 1)",
    Callback = function()
        safe(function()
            local playerGui = LocalPlayer.PlayerGui
            local questGui = playerGui:FindFirstChild("QuestTake")
            
            if questGui then
                local accept0 = questGui:FindFirstChild("Accept0")
                if accept0 then
                    local re0 = accept0:FindFirstChildOfClass("RemoteEvent")
                    if re0 then re0:FireServer() end
                end
                
                local accept1 = questGui:FindFirstChild("Accept1")
                if accept1 then
                    local re1 = accept1:FindFirstChildOfClass("RemoteEvent")
                    if re1 then re1:FireServer() end
                end
                
                OrionLib:MakeNotification({Name = "Test", Content = "Đã thử nhận Accept0 & Accept1", Time = 4})
            else
                OrionLib:MakeNotification({Name = "Không tìm thấy", Content = "GUI QuestTake chưa hiện", Time = 4})
            end
        end)
    end    
})

-- [AUTO SKILL giữ nguyên]
local function makeSkillToggle(tab, key)
    tab:AddToggle({
        Name = "Auto Skill " .. key,
        Default = false,
        Callback = function(state)
            getgenv()["autoskill" .. key:lower()] = state
            task.spawn(function()
                while getgenv()["autoskill" .. key:lower()] do
                    task.wait()
                    safe(function()
                        for _, tool in pairs(LocalPlayer.Character:GetChildren()) do
                            if tool:IsA("Tool") then
                                local skill = tool:FindFirstChild(key)
                                if skill then
                                    for _, remote in pairs(skill:GetChildren()) do
                                        if remote:IsA("RemoteEvent") then
                                            remote:FireServer()
                                        end
                                    end
                                end
                            end
                        end
                    end)
                end
            end)
        end
    })
end

for _, key in ipairs({"Z","X","C","V","B","N"}) do
    makeSkillToggle(SkillTab, key)
end

-- [FRUIT giữ nguyên]
FruitTab:AddButton({
    Name = "STORE AND EAT (4 GUI) 👺",
    Callback = function()
        safe(function()
            local Save = game:GetService("ReplicatedStorage"):WaitForChild("Save")
            local Tool = LocalPlayer.Character:FindFirstChildOfClass("Tool")
            Save:WaitForChild("SavingMelee"):FireServer(-5)
            Save:WaitForChild("SavingSword"):FireServer(-5)
            if Tool and Tool.Name:lower():find("fruit") then 
                Save:WaitForChild("SavingEvent"):FireServer(1) 
            end
            task.wait(0.2)
            for _, Name in pairs({"BOXGUI", "ArmorBox", "DFGUI", "BOOKGUI"}) do
                local Gui = LocalPlayer.PlayerGui:FindFirstChild(Name)
                if Gui and Gui:FindFirstChild("Frame") and Gui.Frame:FindFirstChild("Sure") then
                    if Gui.Frame.Sure:FindFirstChild("EatScript") then 
                        Gui.Frame.Sure.EatScript.Eat:FireServer() 
                    end
                end
            end
        end)
    end
})

-- [LUCKY SPIN giữ nguyên]
local rollAmount = 1
RandomTab:AddTextbox({
    Name = "Số lượng roll",
    Default = "1",
    Callback = function(v) rollAmount = tonumber(v) or 1 end
})
RandomTab:AddButton({
    Name = "Lucky Spin 👺",
    Callback = function()
        safe(function()
            local args = { { true, { Legendary = false, Common = false, Rare = false, Mythical = false, Secret = false }, rollAmount } }
            LocalPlayer.PlayerGui.Random.Bright:FireServer(unpack(args))
        end)
    end
})

-- [AUTO NHẶT - KHÔNG QUÉT MAP & NPCS]
MiscTab:AddToggle({
    Name = "Auto Nhặt Item 100k Stud (0.3s) - Tránh MAP & NPCS",
    Default = false,
    Callback = function(state)
        getgenv().AutoCollectItems = state
        task.spawn(function()
            while getgenv().AutoCollectItems do
                safe(function()
                    local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                    if not hrp then return end
                    
                    for _, obj in pairs(workspace:GetChildren()) do
                        -- Bỏ qua các folder MAP, NPCS, NPC DAMAGE
                        if obj.Name == "MAP" or obj.Name == "NPCS" or obj.Name == "NPC DAMAGE" then
                            continue
                        end
                        
                        -- ClickDetector
                        local cd = obj:FindFirstChildWhichIsA("ClickDetector", true)
                        if cd then
                            local parentPart = cd.Parent
                            if parentPart:IsA("BasePart") and (parentPart.Position - hrp.Position).Magnitude <= 100000 then
                                fireclickdetector(cd)
                            end
                        end
                        
                        -- TouchInterest
                        local touch = obj:FindFirstChildWhichIsA("TouchInterest", true)
                        if touch then
                            local h = touch.Parent
                            if h:IsA("BasePart") and (h.Position - hrp.Position).Magnitude <= 100000 then
                                h.CFrame = hrp.CFrame
                            end
                        end
                    end
                    
                    -- Quét sâu hơn (GetDescendants) nhưng loại trừ MAP & NPCS
                    for _, obj in pairs(workspace:GetDescendants()) do
                        if obj:IsDescendantOf(workspace.MAP) or obj:IsDescendantOf(workspace.NPCS) or obj:IsDescendantOf(workspace["NPC DAMAGE"]) then
                            continue
                        end
                        
                        local cd = obj:FindFirstChildWhichIsA("ClickDetector")
                        if cd then
                            local parentPart = cd.Parent
                            if parentPart:IsA("BasePart") and (parentPart.Position - hrp.Position).Magnitude <= 100000 then
                                fireclickdetector(cd)
                            end
                        end
                        
                        local touch = obj:FindFirstChildWhichIsA("TouchInterest")
                        if touch then
                            local h = touch.Parent
                            if h:IsA("BasePart") and (h.Position - hrp.Position).Magnitude <= 100000 then
                                h.CFrame = hrp.CFrame
                            end
                        end
                    end
                end)
                task.wait(0.3)
            end
        end)
    end
})

-- Load ban đầu
refreshTargetList()

print("Menu ALL-IN-ONE updated! Auto Quest kiểu cũ (Accept0 & 1), Auto nhặt tránh MAP & NPCS.")
